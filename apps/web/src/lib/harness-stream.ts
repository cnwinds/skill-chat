/** HarnessKit stream state and SSE hook — re-exported from @skillchat/harness-react. */
export {
  useStreamUiStore,
  useSessionStream,
  setHarnessChatTestContext,
  clearHarnessChatTestContext,
  defaultHarnessChatTestContext,
} from '@skillchat/harness-react';

export type { StreamStatus, SessionStreamState } from '@skillchat/harness-react';
